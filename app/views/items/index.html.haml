.wrapper 
  = render "shared/left-bar"
  .main
    .main__title -- Cloz --
    .user__name
      = current_user.name
      さんのクローゼット
      = fa_icon "arrow-down"
    .main__content
      .items
        - @items.each do |item|
          .item
            .item__image
              %img{src: "#{item.image}"}
            .item__name
              = item.name